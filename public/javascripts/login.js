/* This script will open the log in dialogue and handle client side log in things */

var loginButtonClicked = function() {
    //make stuff appear
    $('.blackBackground').fadeIn(500);
    $('.loginSomething').fadeIn(500);

    console.log("Login button clicked.");
}
